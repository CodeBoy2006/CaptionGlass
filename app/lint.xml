<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Versions are deliberately pinned as a compatible build set; upgrades are separate work. -->
    <issue id="AndroidGradlePluginVersion" severity="informational" />
    <issue id="GradleDependency" severity="informational" />
    <issue id="NewerVersionAvailable" severity="informational" />
    <!-- API 36 is within AGP 8.13's supported range; evaluate newer SDKs together. -->
    <issue id="OldTargetApi" severity="informational" />
    <!-- M1 explicitly supports arm64 Android phones only, not ChromeOS. -->
    <issue id="ChromeOsAbiSupport" severity="ignore" />
</lint>
