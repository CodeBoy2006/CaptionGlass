# CaptionGlass 产品定义

## 平台与定位

Android 10+ 的本地优先跨应用双语字幕层。原生 Kotlin、Compose；一个采集服务拥有一个识别流和一个翻译工作队列。名称沿用当前项目目录 CaptionGlass，应用 ID 暂定 `com.captionglass.app`，正式分发前确认。

服务于观看外语课程、技术分享和长视频的用户：让用户连续看完内容，而不是不断等待字幕、追逐改写或配置模型。主要差异化假设是阅读体验和持续性能，尚无实测证明优于竞品。

## 产品必须解决的问题

1. **先听见，再读懂。** 临时原文及时显示，译文等待足够完整的语义单位；识别稳定与翻译可提交是不同条件。
2. **读过的内容可信。** 不把翻译 token 直接打到屏幕上，不反复重译历史；重大错误应有显式修正途径。
3. **长时间跟得上。** 控制积压、内存和热量。模型能加载不等于能持续实时运行。
4. **失败可理解。** 静音不等于 DRM；无音频、授权结束、翻译落后和模型未准备好是不同状态。
5. **默认保持本地。** 不隐式转云端、不默认保存音频；文字记录需用户明确选择。

## 用户流程

目标流程：选择语言 → 准备经过验收的语言包 → 检查设备表现 → 开启时请求录音、悬浮窗和本次捕获授权 → 双语字幕 → 停止并释放资源。

主界面保持字幕、记录、设置三个区域；首页使用语言和运行状态表达能力，不展示量化、后端或线程参数。字幕默认译文在上且较大，原文在下且较小，使用系统字体并支持字体缩放。

回看只冻结阅读位置；暂停停止处理；停止结束整个会话。三者不能共用一个含糊的“暂停”状态。跨应用字幕无法控制任意播放器，也无法保证获取源视频时间轴；导出应使用采集会话时间。

## 范围与阶段

| 阶段 | 交付内容 | 明确边界 |
| --- | --- | --- |
| M0 已完成骨架 | 三模块构建、核心回归、架构与验收计划、CI | 原有固定文本预览已被真实链路替换 |
| 当前 M1 单路中英闭环 | X-ASR 480 ms、Hy-MT2 Q4、16/48 kHz 输入、前台服务、基础悬浮窗、校验后离线导入 | arm64 CPU；各一个 ASR/MT；实验版本，记录仅在内存 |
| M2 可持续使用 | Room 记录、DataStore 设置、回看/导出/术语、完整下载管理、30–60 分钟实测 | 记录可关闭，原始音频不落盘 |
| M3 已验证扩展 | 日语、经过验收的省电配置、后端白名单 | 不按内存容量猜测设备性能 |

M1 的验收使用明确标注的合成音频，以真实语速进入实际推理链。它验证闭环和故障处理，不替代多说话人、口音、噪声与长时质量验收。指定真机存在错词和原文首显延迟差距，见验收记录；尚未提供的功能不接入成功占位实现。

## 不可破坏的约束

- 已确认片段必须得到翻译或明确的未翻译结果；队列满不能静默丢弃。
- 所有异步结果绑定会话、片段和版本；旧会话不能覆盖新字幕。
- 原文提交不重置 ASR 声学上下文；模型切换只在片段边界执行，并设冷却。
- 不用提前切断否定、数字或专名换取漂亮延迟，不用摘要换取更少字幕。
- 目标应用可能禁止第三方捕获；系统字幕的权限覆盖率不能作为本应用承诺。
- 不申请通知读取、无障碍、账号、屏幕图像访问或媒体控制权限来补足核心流程。
- 模型包只包含数据，不下载或执行原生代码；运行库由应用构建提供。

## 待验证假设

英语片段以 2–4 秒起测，最长等待约 5–6 秒；日语需独立校准。翻译平均耗时/片段到达间隔以 ≤0.6 为初始目标。原文 P50 0.8 秒、P95 1.2 秒，片段结束到稳定译文 P50 1.5 秒、P95 2.5 秒均是目标，不是测量值。

8GB 测试机应用峰值 PSS 初始预算为 2.5–3GB，必须与视频播放同时验证。准确性、完整性、可读性、积压趋势和功耗共同决定是否开放语言包，不能只报 tokens/s。

## 后续选择

首版不做双 ASR 复核、降噪/声源分离、OCR、RAG、模型市场、云端兜底或完整播放器。后续可评估用户主动导入媒体的可控播放模式，复用翻译和调度并提前处理声音。

产品材料来自用户提供的完整方案；本仓库将方案沉淀为实现约束，不将模型卡或作者的性能描述当作真机验收结果。原始材料中的候选方案不等于发布承诺，技术来源和接入门槛见 [架构](docs/architecture.md) 与 [验收计划](docs/validation.md)。
