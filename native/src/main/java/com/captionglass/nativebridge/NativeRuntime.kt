package com.captionglass.nativebridge

/** No native library or model is bundled until its pinned build passes device validation. */
object NativeRuntime {
    const val isIntegrated: Boolean = false
}
